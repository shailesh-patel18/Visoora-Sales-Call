# ⚠️ DEPRECATED DIRECTORY

This directory (`backend/server/`) contains the legacy v1 MVP architecture.
It is currently running in production, but is officially in a **Code Freeze**.

- **Do NOT** add new features here.
- **Do NOT** refactor this code.

All new development and feature migrations must happen in the `backend/v2/` directory.
Once the `v2` architecture has been verified via Shadow Mode and full traffic migration is complete, this directory will be permanently deleted.
